# Project 08: PLACEHOLDER -- not written yet
#
# The old P08 "Move With Code" moved to P04 on 2026-08-02. Ray's reason:
# drive-a-square landed right after the sumo tournament, which made it a
# Term 1 skill sitting at the top of Term 2. P13 still needs the moving
# skills for its avoid maneuver, and it gets them from P04 now.
#
# This slot is deliberately empty. It sits between P07 Sumo Skills
# (Capstone 1) and P09 Robot Timers, so whatever lands here should be a
# step UP from a competition robot, not a step down.
#
# One candidate, from Ray's own init_bot/nhs_robot/demo/touch_move.py:
# record a sequence of moves with the touch pads, then press OK and the
# robot plays the whole sequence back. That would teach LISTS, which the
# course currently never teaches anywhere, and it reuses P04's
# alvik.move() and alvik.rotate() instead of introducing anything.
#
# Do not print a guide for this project.
